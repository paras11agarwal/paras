from __future__ import unicode_literals

from django.db import models
'''
# Create your models here.
class Detail(models.Model):
	email=models.CharField(max length=200)
	password=models.Charfield(max length=200)
'''